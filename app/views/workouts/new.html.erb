<h1>New Workout</h1>

<%= form_with(model: [ @workout_program , @workout], local: true) do |form| %>
<%= form.text_field :categories %>
<%= form.date_field :date %>
  <h2>Exercises</h2>
  <% Exercise.all.each do |exercise| %>
    <div>
      <%= form.check_box :exercise_ids, { multiple: true }, exercise.id, nil %>
      <%= exercise.name %>
    </div>
  <% end %>

  <%= form.submit %>
<% end %>
